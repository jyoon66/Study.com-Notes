## Quiz
1. A developer wants to display "Guest" if the variable username is empty. Which line of code uses a ternary operator correctly for this purpose?
   * display_name = username if username else "Guest"
2. What is the primary purpose of an if-else statement in programming?
   * To decide between two actions based on a condition
3. A programmer writes the following line of code: grade = "A" if score > 90 else "B" if score > 80 else "C". What is the best recommendation based on best practices?
   * Rewrite it using an if-else statement for better clarity
4. What does a ternary operator allow a programmer to do?
   * Write a one-line conditional assignment or return
5. Which of the following best demonstrates a ternary operator in Python?
   * result = "Pass" if score > 60 else "Fail"