## Quiz

1. What does the append() method do?
   * It can add an object to the end of a list.
2. What does the count() method do?
   * Counts the total number of a specified object in a list.
3. What will result in an error when using the list sort() method?
   * Trying to sort a list that contains character strings and numbers.
4. Which Python list methods are similar?
   * insert() and append()
5. What is the proper way to insert '5' into the 5th position of the MyList that contains '0','1','2','3','4','6','7','8','9','10'
   * MyList.insert(5, '5')