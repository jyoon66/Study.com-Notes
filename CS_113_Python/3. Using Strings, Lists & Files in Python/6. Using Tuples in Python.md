## Quiz
1. Which of the following would be an appropriate reason for using a tuple in Python?
   * Better performance
2. Which of the following is a correct implementation of a tuple in Python?
   * new_tuple = ("Paul", "John", "63")
3. Why would the following code fail on the tuple?
    ```py
    my_tuple = ('L', 'M', 'N', 'O', 15)

    print(min(my_tuple))
    ```
    * The values are not all numeric
4. Which of the following would be the most appropriate use case for a Python tuple?
   * Keys on a piano
5. Which term means unchangeable?
   * Immutable