## Quiz
1. To add an arrow to the right side of the Line object using GraphWin, the setArrow method parameter is a String value _____.
   * last
2. To draw an Oval object using GraphWin the constructor requires _____ parameter(s).
   * Two Point objects
3. To draw a Circle object using GraphWin the constructor requires _____ parameter(s).
   * One Point object and radius value
4. To draw a Text object using GraphWin the constructor requires _____ parameter(s).
   * Point object and the string
5. To draw a Rectangle object using GraphWin the constructor requires _____ parameter(s).
   * Two Point objects