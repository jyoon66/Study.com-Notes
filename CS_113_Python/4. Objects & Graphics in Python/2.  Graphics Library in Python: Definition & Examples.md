## Quiz
1. What widget can we use to allow a user to enter a value?
   * Entry
2. What is the grid location of 'Label' if you use the following code?
```py
self.label = Label(master, text = ''Hello'')
self.label.grid(row=10, column=40)
```
   * (10,40)
3. What would happen if our program did not call mainloop() method for Tk instance?
   * Program would end
4. What widget can we use that when clicked can cause some action to take place?
   * Button
5. What widget can we use to display text data?
   * Label