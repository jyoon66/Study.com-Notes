## Quiz

1. In Python, given the variable strString equals 'Study.com is a wonderful resource.', what will the following code print?
    ```py
    print(strString.find('great'))

    print(strString.find('wonderful'))

    print(strString.find('.'))
    ```
   * -1 15 5
2. What is the index of the first character in a string?
   * 0
3. In Python, given the variable strString equals 'Study.com is a wonderful resource.', which of the following will print the word wonderful?
   * print(strString[15:24])
4. In Python, given the variable strString equals 'Study.com is a wonderful resource.', what will the following print?
    ```py
    print(strString[15:24].find('der') + len(strString))
    ```
   * 37
5. What will the following Python code print?
    ```py
    print(len('Hello world!'))
    ```
    * 12